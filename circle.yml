machine:
  timezone:
    Asia/Tokyo
  ruby:
    version: 2.3.1
dependencies:
  cache_directories:
    - 'vendor/bundle'
test:
  pre:
    #- bundle exec rubocop
deployment:
  production:
    test:
      override:
        -
    branch: master
    commands:
      - bundle exec cap production deploy
